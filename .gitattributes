# 仓库内统一 LF，检出时交由各平台的 core.autocrlf 处理。
* text=auto eol=lf

# 二进制，不做行尾转换、不做 diff。
*.glb  binary
*.jpg  binary
*.jpeg binary
*.png  binary
*.webp binary
*.avif binary
*.pdf  binary

# 模型与锁文件不参与 GitHub 的语言统计，否则语言饼图会被它们带偏。
*.glb            linguist-vendored
package-lock.json linguist-generated

# Shell 脚本必须是 LF，Windows 上检出成 CRLF 会让解释器行为异常。
*.sh text eol=lf
